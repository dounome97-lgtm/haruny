RULES : 
  - Thinking in english and respond in korean.
  - 경로를 지정하지 않는 임시 파일의 생성은 모두 work/tmp 폴더에 생성한다.
  - **DB 데이터 조작 주의점**: psql(mcp 등)을 사용하여 직접 DB에 `DELETE` 또는 `UPDATE` 등의 데이터를 변경하는 조작을 실행하지 않습니다. 치명적인 실수를 방지하기 위해 SQL 쿼리문만 사용자에게 제공하고, 실제 트랜잭션 등 데이터 수정/삭제 작업은 사용자가 직접 실행하도록 합니다.
  - UI/UX, 화면 설계, 기능 구현 작업 전에는 `work/plans/01_프로젝트 개요.md`를 먼저 읽고, 특히 백오피스화 방지 원칙을 항상 적용한다.
  - 작업 속도 원칙: 빈줄 제거, 문구 수정, 단순 스타일 조정, 작은 마크업 변경처럼 영향 범위가 명확한 요청은 관련 파일만 확인하고 빠르게 처리한다. 이런 경우 전체 빌드/전체 테스트는 사용자가 요청했거나 변경 영향이 넓을 때만 실행한다.
  - 검증 강도는 변경 규모에 맞춘다. 작은 변경은 파일 확인 또는 부분 확인으로 끝내고, 라우팅/상태관리/API/DB/공통 컴포넌트/빌드 설정을 건드린 경우에만 전체 빌드나 테스트를 우선 고려한다.
  - 여러 작은 요청을 처리할 때는 매번 전체 체크를 반복하지 말고 빠르게 누적 반영한 뒤, 의미 있는 작업 단위가 끝났을 때 전체 체크를 제안하거나 실행한다.

